{
    "type": "project",
    "license": "open",
    "minimum-stability": "stable",
    "prefer-stable": true,
    "require": {
        "php": ">=7.3.0"
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
    ,"autoload": {
       "psr-0": {"Tiras": "src/"}
    }
}
